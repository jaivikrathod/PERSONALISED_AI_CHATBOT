# ----------------------------------------------------------------------------
# TEMPSERVER — Django backend dependencies
# Python 3.12+ recommended. Pin exact versions in production.
# ----------------------------------------------------------------------------

# Core
Django>=5.2,<5.3
djangorestframework>=3.15
django-cors-headers>=4.4

# WebSockets (chatbot socket between the React client and Django)
channels>=4
daphne>=4
# Redis channel layer - required for more than one worker process
channels-redis>=4.2

# Database (PostgreSQL) + pgvector for indexed similarity search
psycopg2-binary>=2.9
pgvector>=0.3

# Config (.env loading)
python-dotenv>=1.0

# Embeddings (all-MiniLM-L6-v2, runs locally on CPU)
sentence-transformers>=2.2

# LLM answer generation (Gemini)
google-genai>=1.0
